import { IMessageBarStyles } from '@fluentui/react';

export const MessageBarStyles: IMessageBarStyles = {
    content: {
        alignItems: 'center',
    },
    innerText: {
        width: '100%',
    },
    root: {
        borderRadius: '12px',
    },
};
